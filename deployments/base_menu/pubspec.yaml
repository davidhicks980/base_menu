name: base_menu
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.0-228.0.dev

dependencies:
  flutter:
    sdk: flutter
  example:
    path: ../../example
  firebase_core: ^4.11.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
