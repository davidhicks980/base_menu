name: base_menu_gallery_deployment
description: "A deployment of the Base Menu Gallery."
publish_to: 'none'
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.11.4
  flutter: ">=3.44.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  base_menu_gallery:
    path: ../../gallery
  firebase_core: ^4.11.0
  flutter_web_plugins:
    sdk: flutter
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


