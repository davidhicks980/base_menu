name: base_menu_samples_explorer
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.11.4
  flutter: ">=3.44.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  base_menu_demo:
    path: ../../demo
  firebase_core: ^4.11.0
  flutter_web_plugins:
    sdk: flutter
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
