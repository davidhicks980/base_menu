name: floogle_docs
description: "A rich text editor demo demonstrating how to use the Base Menu Library."
publish_to: 'none'
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  base_menu_demo:
    path: ../../demo
  firebase_core: ^4.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0