name: base_menu
description: "Composable widgets for building menu systems."
version: 0.1.0-dev.1
homepage:
repository: https://github.com/davidhicks980/base_menu/tree/main 
issue_tracker: https://github.com/davidhicks980/base_menu/issues

topics:
  - menus
  - ui

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
